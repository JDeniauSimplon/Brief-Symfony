{% extends 'base.html.twig' %}

{% block title %}Log in!{% endblock %}

{% block body %}
    <div class="ridesContainer">
        <form method="post">
            {% if error %}
                <div class="alert alert-danger">
                    {{ error|trans({'%email%': last_username}) }}
                </div>
            {% endif %}

            <h1 class="h3 mb-3 font-weight-normal">SE CONNECTER</h1><p class="emojiVerrou">🔓</p>
            <div class="form-group">
                <label for="inputEmail">Email</label>
                <input type="email" value="{{ last_username }}" name="email" id="inputEmail" class="form-control" autocomplete="email" required autofocus>
            </div>
            <div class="form-group">
                <label for="inputPassword">Mot de passe</label>
                <input type="password" name="password" id="inputPassword" class="form-control" autocomplete="current-password" required>
            </div>
            <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">

            <button class="boutonRedirect" type="submit">
                Se connecter
            </button> <p> ou </p>
            <a class="boutonRedirect" href="{{ path('app_register') }}" class="btn">S'inscrire</a>
        </form>
    </div>
{% endblock %}


